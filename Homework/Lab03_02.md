## CODE
```c++
#include <stdio.h>

int main() {
    int N, i, quantity;
    float unitPrice, itemCost, grandTotal = 0.0;

    if(scanf("%d", &N) != 1) {
        return 1;
    }

    for(i = 0; i < N; i++) {
        scanf("%f %d", &unitPrice, &quantity);

        if(unitPrice >= 1000) {
            grandTotal += unitPrice * 0.90;
        } else {
            grandTotal += unitPrice;
        }
    }


    printf("Grand Total: %.2f\n", grandTotal);
    return 0;
}
```
