## CODE
```c++
#include <stdio.h>
#include <stdlib.h>


void GetSet( int *a[], int *num);

int main() {
    int *data, num;
    GetSet( &data, &num);
    return 0;
}

void GetSet( int *a[], int *num) {
    int row;
    int col;
    printf("Enter array :");
    scanf("%d", &row);
    scanf("%d", &col);
    *a = new int[ row * col];

    for(int i = 0 ; i < row ; i++) {
        for(int j = 0; j < col; j++) {
            printf("a[%d][%d] = ", i, j);
            scanf("%d", &((*a)[ i * col + j]));

        }
    }

}

```
