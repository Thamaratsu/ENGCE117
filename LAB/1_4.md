## CODE
```c++
#include <stdio.h>

int GetSet(int *a[]);

int main() {
    int *data, num;
    num = GetSet(&data);
    return 0;
}

int GetSet(int *a[]) {
    int row;
    int col;
    printf("Enter Array ; \n");
    scanf("%d", &row);
    scanf("%d", &col);
    *a = new int[row * col];

    for(int i = 0; i < row; i++) {
        for(int j = 0; j < col; j++){
            printf("a[%d][%d]\n", i,j);
            scanf("%d", &(*a)[ i * col + j ]);
        }
    }
    return row * col;
}
```
